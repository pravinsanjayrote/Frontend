// let user = 'Pravin Rote';
// let city = 'Pune';
// console.log(`hello ${user} your stay is in ${city}`);
// console.log(`hello '${user}' your stay is in '${city}'`);
// console.log(`hello "${user}" your stay is in "${city}"`);

// let address = `Hello ${user} currently you are staying in ${city}`;

// console.log(address);

let firstName='Pravin';
let lastName='Rote';

function fullname(firstName, lastName){
    return `${firstName} ${lastName}`;
}

let userDetail=`Hello ${fullname(firstName,lastName)}`;
console.log(userDetail);
